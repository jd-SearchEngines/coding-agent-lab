# case_001 — normalize a display name

- `case_type`: synthetic
- `source_repo`: local teaching fixture
- `issue_url`: null
- `base_commit`: repository commit containing this fixture
- `license`: MIT (this repository)
- `expected_behavior`: collapse repeated whitespace, trim, and case-fold a name
- `verification`: `pytest -q`

This is intentionally marked synthetic. It is a small deterministic smoke case,
not a historical GitHub issue reproduction.

